mutation rejectCopilotAccessRequest(
  $input: ResolveCopilotAccessRequestInput!
) {
  rejectCopilotAccessRequest(input: $input) {
    id
    status
    resolvedByUserId
    resolutionReason
    resolvedAt
    updatedAt
  }
}
