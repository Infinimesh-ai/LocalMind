query officeCollaborators($workspaceId: String, $artifactId: String!, $owner: OfficeCommentOwnerInput) {
  officeCollaborators(workspaceId: $workspaceId, artifactId: $artifactId, owner: $owner) {
    id
    name
    avatarUrl
  }
}
