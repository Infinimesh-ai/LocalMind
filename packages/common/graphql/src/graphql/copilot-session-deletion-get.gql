#import "./fragments/copilot-session-deletion.gql"

query copilotSessionDeletionGet($sessionId: ID!) {
  currentUser {
    copilot {
      sessionDeletion(sessionId: $sessionId) {
        ...CopilotSessionDeletionFields
      }
    }
  }
}
