query previewOfficeCommand($input: OfficeCommandInput!) {
  previewOfficeCommand(input: $input) {
    artifactId
    expectedRevisionId
    packageFingerprint
    stateFingerprint
    stats
    summary
  }
}
