#import "./fragments/copilot-context-compaction.gql"

mutation copilotContextCompactionRequest($sessionId: ID!) {
  requestCopilotContextCompaction(sessionId: $sessionId) {
    ...CopilotContextCompactionFields
  }
}
