#import "./fragments/office-comment-fields.gql"

query officeComments($workspaceId: String!, $artifactId: String!) {
  officeComments(workspaceId: $workspaceId, artifactId: $artifactId) {
    ...OfficeCommentFields
  }
}
