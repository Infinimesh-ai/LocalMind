#import "./fragments/office-comment-fields.gql"

query officeComments($workspaceId: String, $artifactId: String!, $owner: OfficeCommentOwnerInput) {
  officeComments(workspaceId: $workspaceId, artifactId: $artifactId, owner: $owner) {
    ...OfficeCommentFields
  }
}
