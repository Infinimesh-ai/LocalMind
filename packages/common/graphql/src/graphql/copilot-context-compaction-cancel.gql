#import "./fragments/copilot-context-compaction.gql"

mutation copilotContextCompactionCancel($taskId: ID!) {
  cancelCopilotContextCompaction(taskId: $taskId) {
    ...CopilotContextCompactionFields
  }
}
