#import "./fragments/copilot-context-compaction.gql"

mutation copilotContextCompactionRetry($taskId: ID!) {
  retryCopilotContextCompaction(taskId: $taskId) {
    ...CopilotContextCompactionFields
  }
}
